# SlotSignalsBot - Telegram Bot для Сигналов Слотов

Чистая архитектура Telegram бота на Python с aiogram 3.x и polling для монетизации трафика казино.

## 🚀 Быстрый старт

### Локальное развертывание

1. **Клонировать репозиторий:**
```bash
git clone <repo-url>
cd slotbot-clean
```

2. **Создать виртуальное окружение:**
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установить зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создать .env файл:**
```bash
cp .env.example .env
# Отредактировать .env и добавить BOT_TOKEN
```

5. **Запустить бота:**
```bash
python main.py
```

## 🔧 Конфигурация

### Переменные окружения (.env)

```env
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
TELEGRAM_CHAT_ID=YOUR_CHAT_ID_FOR_NOTIFICATIONS
```

Получить BOT_TOKEN:
1. Напишите @BotFather в Telegram
2. Используйте `/newbot` для создания нового бота
3. Скопируйте токен

## 📁 Структура проекта

```
slotbot-clean/
├── main.py                 # Главный файл (точка входа)
├── requirements.txt        # Зависимости
├── Procfile               # Railway конфигурация
├── .env.example           # Пример переменных окружения
├── README.md              # Этот файл
└── app/
    ├── __init__.py
    ├── handlers/          # Обработчики команд
    │   ├── __init__.py
    │   ├── start.py       # /start команда
    │   ├── signals.py     # /signals команда
    │   ├── funnel.py      # /funnel команда (воронка продаж)
    │   └── vip.py         # /vip команда (подписка)
    └── services/          # Бизнес-логика
        ├── __init__.py
        ├── vip_service.py         # Управление VIP подписками
        └── postback_service.py    # Обработка постбеков
```

## 🤖 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню и приветствие |
| `/signals` | Текущие сигналы для слотов |
| `/vip` | Информация о VIP подписке |
| `/funnel` | Специальное предложение (воронка продаж) |
| `/help` | Справка по командам |

## 🎯 Функциональность

### VIP Подписка
- Управление VIP статусом пользователей
- Хранение в JSON файле (`vip_users.json`)
- Проверка срока действия подписки
- Поддержка разных периодов подписки

### Постбеки
- Отслеживание действий пользователей
- Интеграция с партнерами (1win, Vavada)
- Расчет заработков
- Хранение в JSON файле (`postbacks.json`)

### Сигналы
- Текущие сигналы для популярных слотов
- Информация о коэффициентах и вероятности
- Доступ для VIP пользователей

## 🚀 Развертывание на Railway

### Требования
- GitHub аккаунт
- Railway аккаунт (https://railway.app)

### Шаги развертывания

1. **Загрузить на GitHub:**
```bash
git add .
git commit -m "Initial commit"
git push origin main
```

2. **Подключить к Railway:**
   - Зайти на https://railway.app
   - Нажать "New Project"
   - Выбрать "Deploy from GitHub repo"
   - Выбрать ваш репозиторий
   - Railway автоматически обнаружит `Procfile`

3. **Добавить переменные окружения:**
   - Перейти в "Variables"
   - Добавить `BOT_TOKEN` с вашим токеном
   - Добавить `TELEGRAM_CHAT_ID` (опционально)

4. **Запустить бота:**
   - Railway автоматически запустит `python main.py`
   - Бот будет работать 24/7

### Мониторинг на Railway

- **Логи:** Перейти в "Deployments" → "View Logs"
- **Статус:** Зеленый статус = бот работает
- **Перезагрузка:** Нажать "Redeploy" для перезагрузки

## 🔍 Отладка

### Проверить, работает ли бот

```bash
# Локально
python main.py

# Должны увидеть:
# ✅ Роутеры зарегистрированы
# ✅ Webhook удален, очередь очищена
# ✅ БОТ ГОТОВ К РАБОТЕ!
```

### Распространенные проблемы

**Проблема:** Бот не отвечает на команды
- **Решение:** Проверить BOT_TOKEN в .env файле
- **Решение:** Убедиться, что бот запущен (`python main.py`)
- **Решение:** Проверить логи на ошибки

**Проблема:** "BOT_TOKEN не установлен!"
- **Решение:** Создать .env файл с BOT_TOKEN
- **Решение:** Убедиться, что .env находится в корне проекта

**Проблема:** На Railway бот не запускается
- **Решение:** Проверить переменные окружения в Railway
- **Решение:** Посмотреть логи развертывания
- **Решение:** Убедиться, что Procfile корректен

## 📊 Архитектура

### Polling vs Webhook

Этот проект использует **polling** (вместо webhook):

**Преимущества polling:**
- ✅ Работает везде (даже за NAT/firewall)
- ✅ Не требует открытого порта
- ✅ Идеален для Railway
- ✅ Простая архитектура

**Недостатки polling:**
- Немного медленнее webhook
- Больше нагрузка на API Telegram

### Минимальная архитектура

- **Один процесс:** Без threading/multiprocessing
- **Только polling:** Без webhook/FastAPI/Flask
- **JSON хранилище:** Без БД (легко масштабировать)
- **Чистый код:** Разделение на handlers/services

## 🔐 Безопасность

- BOT_TOKEN хранится в переменных окружения
- Не коммитить .env файл в Git
- Использовать .gitignore для защиты

## 📝 Лицензия

MIT License

## 🤝 Поддержка

Если у вас есть вопросы или проблемы:
1. Проверьте логи (`python main.py`)
2. Убедитесь, что BOT_TOKEN корректен
3. Проверьте интернет соединение

## 🎓 Дополнительные ресурсы

- [aiogram документация](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Railway документация](https://docs.railway.app/)
